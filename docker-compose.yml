version: "3"

services:
  prairielearn:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PL_COURSE:-./exampleCourse}:/course
    depends_on:
      - db

  db:
    image: postgres